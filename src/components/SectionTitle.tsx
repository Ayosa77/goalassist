export default function SectionTitle({ title }: { title: string }) {
    return (
      <h2 className="text-3xl font-semibold text-gray-100 mb-10 text-center">
        {title}
      </h2>
    );
  }
  